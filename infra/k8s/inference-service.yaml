apiVersion: v1
kind: Service
metadata:
  name: inference-service
  namespace: vision-platform
  labels:
    app: inference
spec:
  type: ClusterIP
  selector:
    app: inference
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP

