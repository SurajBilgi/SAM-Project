apiVersion: v1
kind: Service
metadata:
  name: streaming-service
  namespace: vision-platform
  labels:
    app: streaming
spec:
  type: ClusterIP
  selector:
    app: streaming
  ports:
  - name: http
    port: 8001
    targetPort: 8001
    protocol: TCP

